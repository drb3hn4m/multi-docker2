docker compose -f docker-compose-dev.yml up --build
and then on the browser:
http://localhost:3050/
note that to update the calculated values you need to refresh the page

Username: postgress
password:postgres_password
databasename: fibvalues